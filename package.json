{
  "name": "human-recognition-v1",
  "version": "0.1.0",
  "private": true,
  "description": "Browser-based bedroom camera that identifies household members by height, build, hair length and face, records clips to Supabase, and raises alarms for strangers.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/unit/*.test.js",
    "test:e2e": "node tests/e2e/smoke.mjs",
    "serve": "node tests/e2e/serve.mjs"
  },
  "devDependencies": {
    "playwright": "^1.50.0"
  }
}
